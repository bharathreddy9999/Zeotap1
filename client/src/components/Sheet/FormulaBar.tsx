import React, { useState, useEffect } from 'react';interface FormulaBarProps {  activeCellValue: string;  onFormulaSubmit: (formula: string, result: any) => void;}const FormulaBar: React.FC<FormulaBarProps> = ({ activeCellValue, onFormulaSubmit }) => {  const [formula, setFormula] = useState<string>('');  useEffect(() => {    setFormula(activeCellValue || '');  }, [activeCellValue]);  const handleKeyDown = (e: React.KeyboardEvent<HTMLInputElement>) => {    if (e.key === 'Enter') {      e.preventDefault();      onFormulaSubmit(formula, null);    }  };  return (    <div className="formula-bar">      <div className="formula-icon">=</div>      <input        type="text"        value={formula}        onChange={(e) => setFormula(e.target.value)}        onKeyDown={handleKeyDown}        placeholder="Enter formula or value"        className="formula-input"      />    </div>  );};export default FormulaBar;